> この記事は[セグメンテーション違反](https://ja.wikipedia.org/wiki/セグメンテーション違反)から翻訳されています。


**セグメンテーション違反**（）は[ソフトウェア](../Page/ソフトウェア.md "wikilink")の実行時に起きる特定のエラー条件である。segfault（セグフォールト）と略される場合がある。 セグメンテーション違反はアクセスが許可されていないメモリ上の位置、もしくは許可されていない方法（例えばリードオンリーの位置へ書き込みをしようとする、もしくはオペレーティングシステムの部分を上書きしようとする）でメモリ上の位置にアクセスしようとするときに起こる。[MC68000](../Page/MC68000.md "wikilink")のようなプロセッサに基づくシステムはこれらのイベントを*アドレスエラー*もしくは*バスエラー*として参照しようとする傾向にある。

[セグメント方式](../Page/セグメント方式.md "wikilink")は[メモリ管理](../Page/メモリ管理.md "wikilink")と[オペレーティングシステム](../Page/オペレーティングシステム.md "wikilink")を保護する方法の一つである。大部分の用途のために[ページング方式](../Page/ページング方式.md "wikilink")に置き換えられつつあるが、セグメンテーションの専門用語はまだ多く使用されている。セグメンテーション違反はその一例である。オペレーティングシステムの中にはメインメモリ管理の方針としてページング方法が使用されているがいくつかの論理レベルでまだセグメンテーションを持つものがある。

[UNIX](../Page/UNIX.md "wikilink")ライクのオペレーティングシステム上では、不正なメモリにアクセスをするプロセスは[SIGSEGV](https://ja.wikipedia.org/wiki/SIGSEGV "wikilink")[シグナルを受け取る](../Page/シグナル_\(Unix\).md "wikilink")。[Microsoft Windows上では](https://ja.wikipedia.org/wiki/Microsoft_Windows "wikilink")、不正なメモリにアクセスするプロセスはSTATUS_ACCESS_VIOLATION[例外を受け取る](../Page/例外処理.md "wikilink")。

## 例

次の[ANSI](https://ja.wikipedia.org/wiki/ANSI "wikilink") [C言語](../Page/C言語.md "wikilink")のコードは[メモリ保護機能](https://ja.wikipedia.org/wiki/メモリ保護機能 "wikilink")を持つプラットフォーム上でセグメンテーション違反を作り出す例である。

``` c
const char *s = "hello world";
*s = 'H';
```

このコードを含むプログラムが[コンパイルされた時](../Page/コンパイラ.md "wikilink")、"hello world"の[文字列](../Page/文字列.md "wikilink")リテラルはリードオンリーとしてマークされたプログラム[バイナリのセクションに置かれる](../Page/実行ファイル.md "wikilink"); ロードされたとき、オペレーティングシステムはそれをリードオンリーのメモリーセグメントで他の文字列と[定数](../Page/定数.md "wikilink")データで置き換える。 実行されたとき、*s*変数は、文字列の位置を指定するように設定され、変数を通して*H*文字をメモリに書き込むことが企てられるが、結果はセグメンテーション違反が起こる。このようなプログラムをコンパイルして実行すると、例えば次のような[ランタイムエラーを起こす](../Page/ランタイムライブラリ.md "wikilink"):

``` text
$ gcc segfault.c -g -o segfault
$ ./segfault
Segmentation fault
```

以下は[GNUデバッガ](../Page/GNUデバッガ.md "wikilink")からの[スタックトレース](../Page/スタックトレース.md "wikilink")である:

``` text
Program received signal SIGSEGV, Segmentation fault.
0x1c0005c2 in main () at segfault.c:6
6               *s = 'H';
```

一方で、[Linux](../Page/Linux.md "wikilink")上のgcc 4.1.1はデフォルトでコンパイル時エラーを発生させる:

``` text
$ gcc segfault.c -g -o segfault
segfault.c: In function ‘main’:
segfault.c:4: error: assignment of read-only location
```

セグメンテーション違反が起こる条件とそれらをそれら自身で宣言する方法はオペレーティングシステム特有のものである。

非常に一般的なプログラムエラーは[Null](https://ja.wikipedia.org/wiki/Null "wikilink")ポインタの[参照外し](../Page/参照_\(情報工学\).md "wikilink")（Nullポインタを通した読み込みもしくは書き込み、C言語では"存在しないオブジェクトへのポインタ"を意味し、エラーインディケータとして使用される）であるので、大抵のオペレーティングシステムはアクセスがセグメンテーション違反を引き起こすようなアドレスをNullポインタにマッピングする。

## 関連項目

  - [バッファオーバーラン](https://ja.wikipedia.org/wiki/バッファオーバーラン "wikilink")

  - [バスエラー](../Page/バスエラー.md "wikilink")

  - [コアダンプ](../Page/コアダンプ.md "wikilink")

  - \- このエラーの名前を取ったユーモアウェブサイト。

## 外部リンク

  - [A FAQ: User contributed answers regarding the definition of a segmentation fault](http://www.faqs.org/qa/qa-673.html)
  - [A "null pointer" explained](http://c-faq.com/null/null1.html)
  - [Answer to: NULL is guaranteed to be 0, but the null pointer is not?](http://c-faq.com/null/varieties.html)
  - [Resolving crashes and segmentation faults](http://www.rt-embedded.com/blog/archives/resolving-crashes-and-segmentation-faults/), an article from the Real-Time embedded blog.
  - [The Open Group Base Specifications Issue 6 signal.h](http://www.opengroup.org/onlinepubs/009695399/basedefs/signal.h.html)

[Category:オペレーティングシステムの仕組み](https://ja.wikipedia.org/wiki/Category:オペレーティングシステムの仕組み "wikilink") [Category:コンピュータのエラー](https://ja.wikipedia.org/wiki/Category:コンピュータのエラー "wikilink")