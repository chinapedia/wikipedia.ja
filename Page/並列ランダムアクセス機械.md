> この記事は[](https://ja.wikipedia.org/wiki/)から翻訳されています。


**並列ランダムアクセス機械**（へいれつランダムアクセスきかい、）は、[並列コンピューティング](https://ja.wikipedia.org/wiki/並列コンピューティング "wikilink")に適用可能な[アルゴリズム](../Page/アルゴリズム.md "wikilink")を設計するための[抽象機械](../Page/抽象機械.md "wikilink")である。[同期や](../Page/同期_\(計算機科学\).md "wikilink")[通信](../Page/通信.md "wikilink")といった細かな部分を省き、[並行性](../Page/並行性.md "wikilink")をいかに引き出すかに集中することが可能となる。[フリンの分類](../Page/フリンの分類.md "wikilink")によれば、PRAM は [MIMD](../Page/MIMD.md "wikilink") 型コンピュータに相当する。

## 種類

同期式PRAMでは、複数の[CPU](../Page/CPU.md "wikilink")が同時並行的に[共有メモリ](../Page/共有メモリ.md "wikilink")上の同じ位置にアクセスする可能性がある。PRAMモデルにはいくつかの種類があり、それらの違いは主にそのような同じ位置への同時アクセスを許すか禁止するかである。さらにアクセスには「読み取り」と「書き込み」があるので、以下のような4種類に分類される。

1.  排他的読み取り/排他的書き込み（Exclusive Read Exclusive Write、**EREW**） - 各メモリセルはある時点では1つのプロセッサだけが読み書きできる。
2.  並行的読み取り/排他的書き込み（Conccurrent Read Exclusive Write、**CREW**） - 読み取りは同時に行えるが、書き込みは一度に1つのプロセッサのみである。
3.  排他的読み取り/並行的書き込み（Exclusive Read Conccurrent Write、**ERCW**） - 考慮されない。
4.  並行的読み取り/並行的書き込み（Conccurrent Read Conccurrent Write、**CRCW**） - 複数のプロセッサが自由に読み書きできる。
      - Common CRCW - プロセッサ群が同じ値を同時に書き込むのはOKだが、そうでない場合は不正な操作となる。
      - Arbitrary CRCW - 同時に書き込もうとしたうちの1つだけが成功し、他は失敗する（どれが成功するかは不明）。
      - Priority CRCW - 優先順位によって書き込みが成功するプロセッサが決められる。

問題から並列性を引き出し、分割して並行的にそれを解くことを可能にするアルゴリズムの発見に役立つ。

## 実装

CPU と [DRAM](https://ja.wikipedia.org/wiki/DRAM "wikilink") の組み合わせでは、DRAM が同時アクセスできないため、これらのアルゴリズムを実現できないが、ハードウェアとして実装したり、[FPGA](../Page/FPGA.md "wikilink")で内蔵メモリ([SRAM](../Page/Static_Random_Access_Memory.md "wikilink"))に対して読み書きすると、CRCWになる。

## コード例

これは、わずか2クロックで配列の最大値の値を探す [SystemVerilog](../Page/SystemVerilog.md "wikilink") の例。1クロック目で全ての配列の要素の組み合わせの比較を行い、2クロック目でその結果をマージしている。メモリは Common CRCW で、`m[i] <= 1` と `maxNo <= data[i]` は同時に書き込まれている。アルゴリズムが同じメモリには同じ値を書き込むことを保証しているので問題ない。このプログラムは [FPGA](../Page/FPGA.md "wikilink") 上で実行できる。

``` SystemVerilog
module FindMax #(parameter int len = 8)
                (input bit clock, resetN, input bit[7:0] data[len], output bit[7:0] maxNo);
    typedef enum bit[1:0] {COMPARE, MERGE, DONE} State;

    State state;
    bit m[len];
    int i, j;

    always_ff @(posedge clock, negedge resetN) begin
        if (!resetN) begin
            for (i = 0; i < len; i++) m[i] <= 0;
            state <= COMPARE;
        end else begin
            case (state)
                COMPARE: begin
                    for (i = 0; i < len; i++) begin
                        for (j = 0; j < len; j++) begin
                            if (data[i] < data[j]) m[i] <= 1;
                        end
                    end
                    state <= MERGE;
                end

                MERGE: begin
                    for (i = 0; i < len; i++) begin
                        if (m[i] == 0) maxNo <= data[i];
                    end
                    state <= DONE;
                end
            endcase
        end
    end
endmodule
```

## 関連項目

  - [フリンの分類](../Page/フリンの分類.md "wikilink")
  - [Lock-freeとWait-freeアルゴリズム](../Page/Lock-freeとWait-freeアルゴリズム.md "wikilink")
  - [ランダムアクセス機械](https://ja.wikipedia.org/wiki/ランダムアクセス機械 "wikilink")

## 外部リンク

[University Of Maryland's prototype PRAM](http://www.umiacs.umd.edu/users/vishkin/XMT/spaa07paper.pdf)

## 参考文献

  -
[Category:計算モデル](https://ja.wikipedia.org/wiki/Category:計算モデル "wikilink") [Category:数学に関する記事](https://ja.wikipedia.org/wiki/Category:数学に関する記事 "wikilink")