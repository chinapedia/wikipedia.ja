> この記事は[3番地コード](https://ja.wikipedia.org/wiki/3番地コード)から翻訳されています。


**3アドレスコード**（英: **three-address code**）とは、コンピュータ・[プログラミング言語](../Page/プログラミング言語.md "wikilink")処理系などにおける[中間表現](../Page/中間表現.md "wikilink")などにおける形式の1パターンである。処理系においては、[コンパイラ最適化](../Page/コンパイラ最適化.md "wikilink")などの処理を掛けるのに適している。2つの入力と1つの出力の[アドレス](../Page/メモリアドレス.md "wikilink")（[メモリまたは](../Page/記憶装置.md "wikilink")[レジスタ](../Page/レジスタ_\(コンピュータ\).md "wikilink")）を指定する形式であるため、3アドレスコードと呼ばれる。[命令セット](../Page/命令セット.md "wikilink")アーキテクチャにおける「3オペランド」形式の類推とも言える。

## 概要

この形式における各命令は、形式的に4ツ組で表現すると `(オペコード, ソース1, ソース2, デスティネーション)` である。より直感的にプログラミング言語における代入と2項演算子による数式っぽく書くと、

\[a \oplus b \rightarrow x\] あるいは、

\[x \leftarrow a \oplus b\] といったようになる（ただし \(\oplus\) は、なんらかの演算の2項演算子とする）。「左辺」「右辺」という用語の都合から、ここでは以後、後者の記法を使う。

ここで、a と b は、比較的に具体的な（目的コードに近い）場合は、即値か、レジスタや何らかのアドレスのメモリである。比較的に抽象的な（[ソースコード](../Page/ソースコード.md "wikilink")に近い）場合は、定数か、ソースコード中の変数、あるいはソースコード中にはあらわれない、処理系が生成するテンポラリな中間変数である。また、x は、a や b とは違い、それを対象として代入できるもの（左辺値を持つもの）でなければならなず、即値や定数にはならない。

ソースコード中に次のような、複数の演算で構成された式を含む代入文となっている、1個の文があったとする。

  -
    `x := i + m * n;`

これをそのまま1命令で表現することはできない\[1\]。そのため、次のような2つの命令に分解される。

\[t \leftarrow m \times n\]
\(x \leftarrow i + t\) オペランド数が違っていても「3アドレスコード」という呼び方をすることもある。3アドレスコードの要諦は、各命令が基本的に演算命令1個に相当する演算を表現して\[2\]いなければならずソースやデスティネーションが任意の式ではなく単純な1個の変数などでなければならない点（これは2アドレスコード等でも同様）と、例えば2アドレスコードではデスティネーションをソースの片方と共通としなければならない制限がある（そのオペランドにはメモリが使えずレジスタとするという、より強い制限がしばしば掛かる）のと異なり、任意の組合せが可能な点である。

他に、[プロセッサ](../Page/プロセッサ.md "wikilink")の[命令セット](../Page/命令セット.md "wikilink")における3オペランド形式の場合、[RISC](../Page/RISC.md "wikilink")プロセッサで、命令フォーマットから豊富なメモリアドレッシングを排して空いた空間を利用して、従来の[CISC](../Page/CISC.md "wikilink")プロセッサに多かった2オペランド形式から、より最適化などに適した3オペランド形式としたRISCプロセッサが多いという経緯がある。また例えば、ゼロレジスタ（例えばDEC AlphaプロセッサのR31）を併用すると、

  -
    `y := -x;`

を、

\[y \leftarrow R31 - x\] とできる、などのように3オペランドに統一できる、といったようなことはRISCらしさと言われている一例である。

## 例

``` C
 int main(void)
 {
     int i;
     int b[10];
     for (i = 0; i < 10; ++i) {
         b[i] = i*i;
     }
 }
```

上記の[C言語](../Page/C言語.md "wikilink")プログラムを変換すると、次のようになる。

```
    i := 0                  ; 代入
L1: if i < 10 goto L2       ; 条件付分岐
    goto L3                 ; 無条件分岐
L2: t0 := i*i
    t1 := &b                ; 演算対象のアドレス
    t2 := i << 2            ; bは整数型配列なのでオフセットはi*4(=i<<2)バイト
    t3 := t1 + t2           ; t3 には b[i] のアドレスが設定される
    *t3 := t0               ; ポインタを通して格納する
    i := i + 1
    goto L1
    L3:
```

## 注

<references/>

## 外部リンク

  - [Three-Address Code and Register Allocation](http://cs.wwc.edu/~aabyan/464/Quads.html)
  - [CSc 453: A Three-Address Intermediate Code Instruction Set for C--](http://www.cs.arizona.edu/classes/cs453/fall07/DOCS/intcode.html)

[Category:コンパイラ](https://ja.wikipedia.org/wiki/Category:コンパイラ "wikilink") [Category:名数3](https://ja.wikipedia.org/wiki/Category:名数3 "wikilink")

1.  しばしばある、`x += m * n` のようなソースコードと、積和演算命令に対応した処理系、といったような場合には違ってくるが、ここでは一般的な場合を考えることとする。
2.  ここではロード・ストア命令は考えない。