> この記事は[スタックトレース](https://ja.wikipedia.org/wiki/スタックトレース)から翻訳されています。


[コンピュータ](../Page/コンピュータ.md "wikilink")において、**スタックトレース**（）とは、[プログラムの実行中の特定の時点でのアクティブな](../Page/プログラム_\(コンピュータ\).md "wikilink")[スタックフレーム](https://ja.wikipedia.org/wiki/スタックフレーム "wikilink")のレポートである。**スタックバックトレース**（\[1\]）、**スタックトレースバック**（\[2\]）とも言い、単に**バックトレース**とも言う。

プログラムが実行されると、多くの場合、[スタック](../Page/スタック.md "wikilink")と[ヒープの](../Page/ヒープ領域.md "wikilink")2つの場所に[メモリが動的に割り当てられる](../Page/主記憶装置.md "wikilink")。スタックという言葉はプログラムの構文についても使われるため、区別するために、このスタックは[コールスタック](https://ja.wikipedia.org/wiki/コールスタック "wikilink")(call stack)と呼ばれる。技術的には、メモリブロックがスタックに割り当てられると、その前に他のメモリブロックが割り当てられている可能性があるため、簡単に削除することはできない。プログラムで[関数が呼び出されるたびに](../Page/サブルーチン.md "wikilink")、スタックフレーム（またはアクティベーションレコード）と呼ばれるコールスタックの最上部にメモリブロックが割り当てられる。高レベルでは、スタックフレームは、関数内で宣言された関数のパラメータとローカル変数にメモリを割り当てる。

プログラマは通常、[デバッグ](../Page/デバッグ.md "wikilink")においてスタックトレースを使用する。エンドユーザには、[エラーメッセージ](../Page/エラーメッセージ.md "wikilink")の一部としてスタックトレースが表示されることがあり、障害報告の際に使用する。

スタックトレースを使用すると、スタックトレースが生成されるまでの、呼び出されたのシーケンスを追跡できる。事後分析では、障害が発生した関数まで追跡できる（ただし必ずしも追跡できるわけではない）。[末尾再帰](../Page/末尾再帰.md "wikilink")はスタックトレースに表示されない。

次の、エラーが含まれた[Python](../Page/Python.md "wikilink")プログラムで説明する。

``` python numberLines
def a():
    i = 0
    j = b(i)
    return j

def b(z):
    k = 5
    if z == 0:
        c()
    return k/z

def c():
    error() #存在しない関数を呼び出そうとしている

a()
```

このプログラムを標準のPythonインタプリタで実行すると、次のエラーメッセージが生成される。

``` pytb
Traceback (most recent call last):
  File "tb.py", line 15, in <module>
    a()
  File "tb.py", line 3, in a
    j = b(i)
  File "tb.py", line 9, in b
    c()
  File "tb.py", line 13, in c
    error()
NameError: name 'error' is not defined
```

スタックトレースは、エラーが発生した場所、つまり関数`c`を示している。また、関数`c`は関数`b`から、関数`b`は関数`a`から、関数`a`はプログラムの15行目（最終行）から呼び出されたことも示している。

これらの3つの関数のそれぞれのアクティベーションレコードは、関数`a`がスタックの下部を占有し、関数`c`がスタックの上部を占有するようにスタックに配置される。

## プログラミング言語によるサポート

[Java](https://ja.wikipedia.org/wiki/Java "wikilink")や[C\#](https://ja.wikipedia.org/wiki/C# "wikilink")など多くの[プログラミング言語](../Page/プログラミング言語.md "wikilink")には、システムコールを介して現在のスタックトレースを取得するための機能が組み込まれている。[C++](../Page/C++.md "wikilink")にはこれを行うための組み込みの機能はないが、[stacktrace](http://stacktrace.sourceforge.net/)[ライブラリ](../Page/ライブラリ.md "wikilink")などを使用してスタックトレースを取得できる。[JavaScript](../Page/JavaScript.md "wikilink")では、[例外が](../Page/例外処理.md "wikilink")、スローされた場所からのスタックを含む`stack`プロパティを保持している。

## 関連項目

  - [末尾再帰](../Page/末尾再帰.md "wikilink")
  - [コンテキスト (情報工学)](https://ja.wikipedia.org/wiki/コンテキスト_\(情報工学\) "wikilink")
  - [スタックオーバーフロー](../Page/スタックオーバーフロー.md "wikilink")
  - [例外処理](../Page/例外処理.md "wikilink")

## 脚注

[Category:デバッグ](https://ja.wikipedia.org/wiki/Category:デバッグ "wikilink")

1.
2.