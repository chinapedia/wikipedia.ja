> この記事は[LTIシステム理論](https://ja.wikipedia.org/wiki/LTIシステム理論)から翻訳されています。


**LTIシステム理論**（）は、[電気工学](../Page/電気工学.md "wikilink")、特に[電気回路](../Page/電気回路.md "wikilink")、[信号処理](https://ja.wikipedia.org/wiki/信号処理 "wikilink")、[制御理論](../Page/制御理論.md "wikilink")といった分野で、**[線型](../Page/線形システム論.md "wikilink")[時不変系](../Page/時不変系.md "wikilink")**（**l**inear **t**ime-**i**nvariant system）に任意の入力信号を与えたときの応答を求める理論である。通常、独立変数は時間だが、空間（[画像処理](../Page/画像処理.md "wikilink")や[場の古典論](../Page/場の古典論.md "wikilink")など）やその他の座標にも容易に適用可能である。そのため、**線型並進不変**（linear translation-invariant）という用語も使われる。[離散時間](https://ja.wikipedia.org/wiki/離散信号 "wikilink")（[標本化](https://ja.wikipedia.org/wiki/標本化 "wikilink")）系では対応する概念として**線型シフト不変**（linear shift-invariant）がある。

## 概要

任意の線型時不変系の属性を定義するのは、当然ながら**線型性**（linearity）と**時不変性**（time invariance）である。

**線型性**とは、システムの入力と出力の関係が、重ね合わせ特性を持つことを意味する。システムへの入力が次のように2つの信号を足し合わせたものであるとする。  すると、システムの出力は次のようになる。  ここで、\(y_n(t)\) は入力が \(x_n(t)\) だけだったときの出力を意味する。

このような重ね合わせ特性がある場合、任意の[有理数](https://ja.wikipedia.org/wiki/有理数 "wikilink")スカラーについてスケーリング特性が得られる。入力 \(x(t)\) による出力が \(y(t)\) であるとき、入力 \(c x(t)\) による出力は \(c y(t)\) となる。

以上を形式的に表すと、線型系は次のような特性を示す。まず、システムに次の入力を与えるとする。  すると、そのシステムの出力は次のようになる。  \(c_n\) は任意の定数であり、\(y_n(t)\) は入力が \(x_n(t)\) だけだったときの出力を意味する。

**時不変性**とは、システムにある入力信号を現時点や *T* 秒後に与えたとき、*T* 秒のずれが生じるだけで出力信号が同じになることを意味する。入力 \(x(t)\) による出力が \(y(t)\) であるとき、入力 \(x(t-T)\) による出力は \(y(t-T)\) となる。つまり、入力が遅延すれば、出力もそのぶんだけ遅延する。これを時不変という。

LTIシステム理論の基本的な成果は、任意のLTIシステムを[インパルス応答](https://ja.wikipedia.org/wiki/インパルス応答 "wikilink")と呼ばれる単一の関数で完全に表せるようになったことである。システムの出力は、インパルス応答を持つシステムへの入力の単純な[畳み込み](https://ja.wikipedia.org/wiki/畳み込み "wikilink")である。この解析手法は、[時間領域](../Page/時間領域.md "wikilink")の観点であるといわれることが多い。離散時間線型シフト不変システムでも同様のことが言え、その場合の信号は離散時間の標本群であり、畳み込みはそれらの列に対するものとなる。

[thumb](https://ja.wikipedia.org/wiki/ファイル:LTI.png "wikilink")

これと等価的に、[伝達関数を使ってLTIシステムを](../Page/伝達関数法.md "wikilink")[周波数領域](../Page/周波数領域.md "wikilink")で解析することもできる。伝達関数とは、システムのインパルス応答を[ラプラス変換](https://ja.wikipedia.org/wiki/ラプラス変換 "wikilink")（離散時間の場合は[Z変換](../Page/Z変換.md "wikilink")）したものである。このような変換の特性として、周波数領域のシステムの出力は、入力を変換したものと伝達関数の積で表される。言い換えれば、時間領域での畳み込みと、周波数領域での乗法が等価となっている。

全てのLTIシステムにおいて、[固有関数](../Page/固有関数.md "wikilink")と変換の[基底関数](https://ja.wikipedia.org/wiki/基底関数 "wikilink")は[複素](../Page/複素数.md "wikilink")[指数関数](../Page/指数関数.md "wikilink")である。システムへの入力が複素波形 \(A\exp({st})\)（\(A\) は複素振幅、\(s\) は複素周波数）であるとき、その出力は入力にある複素定数を掛けたもの、例えば \(B\exp({st})\) となり、\(B\) は何らかの新たな複素振幅である。\(B/A\) という比は、周波数 \(s\) における伝達関数である。

[正弦波](../Page/正弦波.md "wikilink")は複素共役周波数の複素指数関数の総和であるため、システムの入力が正弦波なら、そのシステムの出力も正弦波となり、おそらく異なる[振幅](../Page/振幅.md "wikilink")と異なる[位相](../Page/位相.md "wikilink")を持つが、周波数は同じになるだろう。

LTIシステム理論は様々な重要なシステムを説明できる。多くのLTIシステムは解析が「容易」とされており、少なくとも時変系や[非線型のシステムに比べれば単純である](../Page/非線形システム論.md "wikilink")。定数係数の線型な斉次[微分方程式](../Page/微分方程式.md "wikilink")としてモデル化されるシステムは、LTIシステムである。例えば、[抵抗器](../Page/抵抗器.md "wikilink")と[コイル](../Page/コイル.md "wikilink")と[コンデンサ](../Page/コンデンサ.md "wikilink")で構成される[電気回路](../Page/電気回路.md "wikilink")（[RLC回路](https://ja.wikipedia.org/wiki/RLC回路 "wikilink")）がある。また、理想的なバネ-質量-ダンパ系もLTIシステムであり、数学的にはRLC回路と等価である。

多くのLTIシステムの概念は、連続時間と離散時間（線型シフト不変）とで類似している。画像処理では、時間変数は2次元の空間変数に置き換えられ、時不変性に関する事柄は2次元のシフト不変性に関する事柄に置き換えられる。[フィルタバンク](https://ja.wikipedia.org/wiki/フィルタバンク "wikilink")や[MIMO](../Page/MIMO.md "wikilink")を解析する場合、信号の[配列](../Page/配列.md "wikilink")を考えると分かり易い。

## 連続時間システム

### 時間不変性と線型写像

ここでは、時間を独立変数とし、そのインパルス応答が2次元関数であるシステムを想定し、時不変性によってそれを1次元に還元できることを示す。例えば、入力信号 \(x(t)\) において、その[添え字集合が実数線であるとする](../Page/媒介変数.md "wikilink")（すなわち、\(t \in \mathbb{R}\)）。[線型作用素](https://ja.wikipedia.org/wiki/線型写像 "wikilink") \(\mathcal{H}\) はその入力信号に対して処理をするシステムを表している。この添え字集合に対して適切な作用素は、次のような2次元関数である。

\(\mathcal{H}\) は線型作用素なので、入力信号 \(x(t)\) に対するシステムの動作は、以下の[重ね合わせ積分で表される](../Page/線型性.md "wikilink")[線型写像](https://ja.wikipedia.org/wiki/線型写像 "wikilink")となる。

[線型作用素](https://ja.wikipedia.org/wiki/線型写像 "wikilink") \(\mathcal{H}\) が[時不変でもある場合](../Page/時不変系.md "wikilink")、次のようになる。

ここで、次のように設定する。

すると、次のようになる。

\(h (t_1, t_2)\) の第二引数がゼロなら、通常それを簡潔さのために削除するので、上記の重ね合わせ積分はフィルタ設計でよく使われる[畳み込み](https://ja.wikipedia.org/wiki/畳み込み "wikilink")積分になる。

従って、この[畳み込み](https://ja.wikipedia.org/wiki/畳み込み "wikilink")積分は任意の入力関数についての[線型](../Page/線型性.md "wikilink")[時不変系](../Page/時不変系.md "wikilink")の作用を表している。有限次元のアナログについては、[巡回行列](../Page/巡回行列.md "wikilink")を参照されたい。

### インパルス応答

このシステムに[ディラックのデルタ関数](../Page/ディラックのデルタ関数.md "wikilink")を入力したとき、デルタ関数は理想的なインパルスであるため、LTI変換の結果が[インパルス応答](https://ja.wikipedia.org/wiki/インパルス応答 "wikilink")となる。これを式に表すと次のようになる。

これにはデルタ関数のシフト属性を利用している。なお、ここで次が成り立つ。

従って \(h(t)\) はそのシステムのインパルス応答である。

インパルス応答を使うと、任意の入力に対する応答を求めることができる。再び \(\delta(t)\) のシフト属性を使い、任意の入力をデルタ関数群の重ね合わせとして表せる。  この入力をシステムに適用すると、次のようになる。  システムに関する全ての情報は、インパルス応答 \(h(t)\) に含まれている。

### 固有関数としての指数関数

[固有関数](../Page/固有関数.md "wikilink")とは、上述の作用素の出力が入力された関数に何らかのスケーリングを施した同じ関数になるときの入力された関数をいう。数式で表すと次の通り。  ここで、*f* が固有関数であり、\(\lambda\) は[固有値](../Page/固有値.md "wikilink")と呼ばれる定数である。

[指数関数](../Page/指数関数.md "wikilink") \(e^{s t}\)（\(s \in \mathbb{C}\)）は、[線型](../Page/線型性.md "wikilink")[時不変作用素の固有関数である](../Page/時不変系.md "wikilink")。これについての簡単な証明を示す。

入力を \(x(t) = e^{s t}\) とする。インパルス応答 \(h(t)\) でのシステムの出力は次のようになる。

[畳み込み](https://ja.wikipedia.org/wiki/畳み込み "wikilink")の交換律から、これを次のように変形できる。  ここで  はパラメータ *s* にのみ依存する。

従って、システムの応答は入力に定数 \(H(s)\) をかけたものと同じであるから、\(e^{s t}\) はLTIシステムの固有関数である。

### フーリエ変換とラプラス変換

指数関数が固有関数であるという性質は、LTIシステムの解析や予測に役立つ。その[ラプラス変換](https://ja.wikipedia.org/wiki/ラプラス変換 "wikilink")

を使えば、インパルス応答から固有値を得ることができる。特に興味深いのは純粋な正弦波の場合（\(\exp({j \omega t})\) の形式の指数関数、ただし \(\omega \in \mathbb{R}\) であり、かつ \(j = \sqrt{-1}\)）である。これは引数が純粋な虚数であっても、一般に複素指数関数と呼ばれる。[フーリエ変換](../Page/フーリエ変換.md "wikilink") \(H(j \omega) = \mathcal{F}\{h(t)\}\) により、純粋な[複素正弦波](https://ja.wikipedia.org/wiki/複素正弦波 "wikilink")の固有値が求められる。\(H(s)\) と \(H(j\omega)\) は共に**システム関数**、**システム応答**、**伝達関数**などと呼ばれる。

ラプラス変換は一般に、*t* がある値より小さいとき信号がゼロとなるような信号で使われる。通常、その信号がゼロでなくなる時点をスタート時点とし、ゼロから無限大までの積分とする（一方、負の無限大から積分するラプラス変換を一般に「両側ラプラス変換; bilateral Laplace transform」と呼ぶ）。

フーリエ変換は、無限に続く信号を処理するシステムの解析に使われる。例えば、変調された正弦波などだが、二乗可積分でない入力信号や出力信号には直接適用できない。スタート時点以前の信号がゼロなら、ラプラス変換は二乗可積分でなくとも適用可能である、フーリエ変換は、その信号のフーリエ変換が存在しない場合でも、[ウィーナー・ヒンチンの定理](https://ja.wikipedia.org/wiki/ウィーナー・ヒンチンの定理 "wikilink")を使って無限信号のスペクトルに適用される。

これらの変換は畳み込み属性があるため、システムの出力を与える畳み込みを[畳み込み定理](https://ja.wikipedia.org/wiki/畳み込み定理 "wikilink")によって個別に変換したあとに積を求める形に変換できる。

これにより変換や逆変換が容易になるだけでなく、システム応答からシステムの挙動についての洞察を得ることができる。システム関数の絶対値 |*H*(*s*)| から、入力 \(\exp({s t})\) がシステムを通過できるか、それとも減衰してしまうかを見ることができる。

### 例

LTI作用素の簡単な例として[導関数がある](https://ja.wikipedia.org/wiki/微分法 "wikilink")。  導関数のラプラス変換をとってみたとき、ラプラス変数 s によって単純な乗算に変形される。  導関数がこのような単純なラプラス変換の形式となることは、変換の有効性の証でもある。

別の単純なLTI作用素として、平均化作用素がある。

これは、積分が線型性をもつため、線型である。  また、時不変でもある。  \(\mathcal{A}\) は次のような畳み込みとして記述することもできる。

なお \(\Pi(t)\) は次のように定義される。

### 重要なシステム属性

システムについて最も重要な属性として、因果性と安定性がある。実世界でシステムを利用する場合、因果性は多かれ少なかれ必要である。非安定的なシステムも構築でき、様々な状況で有効である。

#### 因果性

出力が現在と過去の入力のみに依存する場合、システムは「因果的; causal」であるという。「因果性; causality」の必要十分条件は次が成り立つことである。

ここで \(h(t)\) はインパルス応答である。ラプラス変換は逆変換が一意に定まらないため、そこから因果性を判断することは通常不可能である。[収束領域が示される場合](../Page/収束半径.md "wikilink")、因果性を判断できる。

#### 安定性

システムが**有界入力-有界出力安定**（[BIBO安定](https://ja.wikipedia.org/wiki/BIBO安定 "wikilink")）であるとは、全ての入力が有界なら出力も有界であることを意味する。数学的には、入力が次の条件を満たすとき

出力が次を満足する。

すなわち、\(x(t)\) の有限の最大絶対値があれば、\(y(t)\) の有限の最大絶対値が存在する。このとき、システムは安定であるという。必要十分条件は、インパルス応答 \(h(t)\) が [L<sup>1</sup>](https://ja.wikipedia.org/wiki/Lp空間 "wikilink") にあることである（有限のL<sup>1</sup>[ノルム](../Page/ノルム.md "wikilink")を持つ）。

周波数領域では、[収束領域に虚数軸](../Page/収束半径.md "wikilink") \(s=j\omega\) が含まれていなければならない。システムを[伝達関数](https://ja.wikipedia.org/wiki/伝達関数 "wikilink")としてモデル化するとき、系の極（伝達関数の分母多項式または[特性多項式](https://ja.wikipedia.org/wiki/特性多項式 "wikilink")の根）を複素平面の左半平面に置かなければならない。[ラウス・フルビッツの安定判別法](https://ja.wikipedia.org/wiki/ラウス・フルビッツの安定判別法 "wikilink")によって特性多項式の係数から安定性が見える。

例としては、インパルス応答が[Sinc関数](../Page/Sinc関数.md "wikilink")と等しい理想的な[ローパスフィルタ](../Page/ローパスフィルタ.md "wikilink")は、BIBO安定ではない。これはSinc関数が有限のL<sup>1</sup>ノルムを持たないためである。従って何らかの有界な入力では、理想的なローパスフィルタの出力は無限となる。特に \(t < 0\,\) のとき入力がゼロで \(t > 0\,\) のとき[カットオフ周波数](https://ja.wikipedia.org/wiki/カットオフ周波数 "wikilink")の正弦波となる場合、出力は原点以外では常に無限となる。

## 離散時間システム

連続時間システムに関するほとんどあらゆる事柄について、離散時間システムにも対応する事柄が存在する。

### 連続時間システムから離散時間システムへ

多くの場合、離散時間 (DT) システムはより大きな連続時間 (CT) システムの一部となっている。例えば、デジタル録音システムはアナログの音響を入力とし、それを[デジタイズ](../Page/デジタイズ.md "wikilink")して、必要に応じてデジタル信号を処理し、最終的に再生して人間が聴くためにアナログに戻してやる。

形式的には、研究されているDT信号のほとんどは、CT信号を一定間隔で[標本化](https://ja.wikipedia.org/wiki/標本化 "wikilink")したものである。CT信号を \(x(t)\) としたとき、[アナログ-デジタル変換回路](../Page/アナログ-デジタル変換回路.md "wikilink")によってそれがDT信号 \(x[n]\) に次のように変換される。  ここで *T* は[サンプリング間隔である](../Page/サンプリング周波数.md "wikilink")。DT信号が元の信号を正確に表現するには、入力信号の周波数の範囲を制限することが非常に重要である。[標本化定理](../Page/標本化定理.md "wikilink")によれば、DT信号は \(1/(2T)\) までの範囲の周波数しか扱えない。さもなくば、高周波成分がその範囲に[折り返し雑音](../Page/折り返し雑音.md "wikilink")として出てくる。

### 時間不変性と線型写像

ここでは、時間を独立変数とし、そのインパルス応答が2次元関数であるシステムを想定し、時不変性によってそれを1次元に還元できることを示す。例えば、入力信号 \(x[n]\) において、その[添え字集合が整数であるとする](../Page/媒介変数.md "wikilink")（すなわち、\(n \in \mathbb{Z}\)）。[線型作用素](https://ja.wikipedia.org/wiki/線型写像 "wikilink") \(\mathcal{H}\) はその入力信号に対して処理をするシステムを表している。この添え字集合に対して適切な作用素は、次のような2次元関数である。

\(\mathcal{H}\) は線型作用素なので、入力信号 \(x[n]\) に対するシステムの動作は、以下の[重ね合わせ総和で表される](../Page/線型性.md "wikilink")[線型写像](https://ja.wikipedia.org/wiki/線型写像 "wikilink")となる。

[線型作用素](https://ja.wikipedia.org/wiki/線型写像 "wikilink") \(\mathcal{H}\) が[時不変でもある場合](../Page/時不変系.md "wikilink")、次のようになる。

ここで、次のように設定する。

すると、次のようになる。

\(h[n_1, n_2]\) の第二引数がゼロなら、通常それを簡潔さのために削除するので、上記の重ね合わせ積分はフィルタ設計でよく使われる[畳み込み](https://ja.wikipedia.org/wiki/畳み込み "wikilink")総和になる。

従って、この[畳み込み](https://ja.wikipedia.org/wiki/畳み込み "wikilink")総和は任意の入力関数についての[線型](../Page/線型性.md "wikilink")[時不変系](../Page/時不変系.md "wikilink")の作用を表している。有限次元のアナログについては、[巡回行列](../Page/巡回行列.md "wikilink")を参照されたい。

### インパルス応答

このシステムに[離散デルタ関数を入力したとき](../Page/ディラックのデルタ関数.md "wikilink")、デルタ関数は理想的なインパルスであるため、LTI変換の結果が[インパルス応答](https://ja.wikipedia.org/wiki/インパルス応答 "wikilink")となる。これを式に表すと次のようになる。

これにはデルタ関数のシフト属性を利用している。なお、ここで次が成り立つ。

従って \(h[n]\) はそのシステムのインパルス応答である。

インパルス応答を使うと、任意の入力に対する応答を求めることができる。再び \(\delta[n]\) のシフト属性を使い、任意の入力をデルタ関数群の重ね合わせとして表せる。

この入力をシステムに適用すると、次のようになる。  システムに関する全ての情報は、インパルス応答 \(h[n]\) に含まれている。

### 固有関数としての指数関数

[固有関数](../Page/固有関数.md "wikilink")とは、上述の作用素の出力が入力された関数に何らかのスケーリングを施した同じ関数になるときの入力された関数をいう。数式で表すと次の通り。  ここで、*f* が固有関数であり、\(\lambda\) は[固有値](../Page/固有値.md "wikilink")と呼ばれる定数である。

[指数関数](../Page/指数関数.md "wikilink") \(z^n = e^{sT n}\)（\(n \in \mathbb{Z}\)）は、[線型](../Page/線型性.md "wikilink")[時不変作用素の固有関数である](../Page/時不変系.md "wikilink")。\(T \in \mathbb{R}\) はサンプリング間隔であり、\(z = e^{sT}, \ z,s \in \mathbb{C}\) である。これについての簡単な証明を示す。

入力を \(x[n] = \,\!z^n\) とする。インパルス応答 \(h[n]\) でのシステムの出力は次のようになる。

[畳み込み](https://ja.wikipedia.org/wiki/畳み込み "wikilink")の交換律から、これを次のように変形できる。  ここで  はパラメータ *s* にのみ依存する。

従って、システムの応答は入力に定数 \(H(z)\) をかけたものと同じであるから、\(z^n\) はLTIシステムの固有関数である。

### Z変換と離散時間フーリエ変換

指数関数が固有関数であるという性質は、LTIシステムの解析や予測に役立つ。その[Z変換](../Page/Z変換.md "wikilink")

を使えば、インパルス応答から固有値を得ることができる。特に興味深いのは純粋な正弦波の場合（\(e^{j \omega n}\) の形式の指数関数、ただし \(\omega \in \mathbb{R}\)）である。これは引数が純粋な虚数であっても、一般に複素指数関数と呼ばれる。[離散時間フーリエ変換](https://ja.wikipedia.org/wiki/離散時間フーリエ変換 "wikilink") (DTFT) \(H(e^{j \omega}) = \mathcal{F}\{h[n]\}\) により、純粋な複素正弦波の固有値が求められる。\(H(z)\) と \(H(e^{j\omega})\) は共に**システム関数**、**システム応答**、**伝達関数**などと呼ばれる。

Z変換は一般に、*t* がある値より小さいとき信号がゼロとなるような信号で使われる。通常、その信号がゼロでなくなる時点をスタート時点とする。フーリエ変換は、無限に続く信号を処理するシステムの解析に使われる。

これらの変換は畳み込み属性があるため、システムの出力を与える畳み込みを[畳み込み定理](https://ja.wikipedia.org/wiki/畳み込み定理 "wikilink")によって個別に変換したあとに積を求める形に変換できる。  これにより変換や逆変換が容易になるだけでなく、システム応答からシステムの挙動についての洞察を得ることができる。システム関数の絶対値 *|H(z)|* から、入力 \(z^n\) がシステムを通過できるか、それとも減衰してしまうかを見ることができる。

### 例

LTI作用素の簡単な例として遅延作用素 \(D\{x\}[n]:=x[n-1]\) がある。  遅延作用素のZ変換をとってみると、z<sup>-1</sup> の単純な乗算に変形される。  遅延作用素がこのような単純なZ変換の形式となることは、変換の有効性の証でもある。

別の単純なLTI作用素として、平均化作用素がある。

これは、総和が線型性をもつため、線型である。  また、時不変でもある。

### 重要なシステム属性

システムについて最も重要な属性として、因果性と安定性がある。CTシステムとは異なり、因果性のないDTシステムも実現可能である。非因果性[FIRシステムに遅延を加えることで](https://ja.wikipedia.org/wiki/有限インパルス応答 "wikilink")、簡単に因果性を持たせることができる。また、非因果性[IIRシステムを作ることもできる](../Page/無限インパルス応答.md "wikilink")（Vaidyanathan and Chen, 1995 を参照）。非安定的なシステムも構築でき、様々な状況で有効である。

#### 因果性

出力が現在と過去の入力のみに依存する場合、システムは「因果的; causal」であるという。「因果性; causality」の必要十分条件は次が成り立つことである。

ここで \(h[n]\) はインパルス応答である。Z変換は逆変換が一意に定まらないため、そこから因果性を判断することは通常不可能である。[収束領域が示される場合](../Page/収束半径.md "wikilink")、因果性を判断できる。

#### 安定性

システムが[有界入力-有界出力安定](https://ja.wikipedia.org/wiki/有界入力有界出力安定性 "wikilink")（BIBO安定）であるとは、全ての入力が有界なら出力も有界であることを意味する。数学的には、入力が次の条件を満たすとき

出力が次を満足する。

すなわち、\(x[n]\) の有限の最大絶対値があれば、\(y[n]\) の有限の最大絶対値が存在する。このとき、システムは安定であるという。必要十分条件は、インパルス応答 \(h[n]\) が次を満足することである。

周波数領域では、[収束領域に単位円](../Page/収束半径.md "wikilink") \(|z|=1\) が含まれていなければならない。システムを伝達関数としてモデル化するとき、系の極を複素平面の単位円に置かなければならない。[ジュリーの安定判別法](https://ja.wikipedia.org/wiki/ジュリーの安定判別法 "wikilink")によって特性多項式の係数から安定性が見える。

##### 二次元安定性

二次元信号（例えば画像）の場合では、二元多項式が必ず因数分解できるとは限らないため、フィルターのBIBO安定性の判定は困難である。

まず、系の伝達関数が\(H(z_1,z_2)=\frac{B(z_1,z_2)}{A(z_1,z_2)}\)として表示されて、以下のように極を分類する：

1.  \(B(z_1,z_2)\)の根と違う\(A(z_1,z_2)\)の根は、第一類非真性特異点（Nonessential Singularities of the First Kind、NSFK）という；
2.  \(B(z_1,z_2)\)の根と重なる\(A(z_1,z_2)\)の根は、第二類非真性特異点（Nonessential Singularities of the Second Kind、NSSK）という。

NSSKはゼロと極を消去できなくで生まれる。例として、伝達関数は </math>}} のようにする。そのゼロは  になり、極は  になるので、\((1,1)\)はNSSKになる。NSSKの存在は複雑性の源。

便利のため、まだ以下の区域を定義する：  ならば、以下の定理が成立する。

  - （Goodman）上記の伝達関数\(H(z_1,z_2)\)に対しては、
    1.  \(A(z_1,z_2)\ne0,(z_1,z_2) \in S_c \Rightarrow\) システムが安定
    2.  システムが安定 \(\Rightarrow A(z_1,z_2)\ne0,(z_1,z_2) \in S_o\)
  - （Huang）\(T\)にNSSKがない時、伝達関数\(H(z_1,z_2)\)は安定する必要十分条件は以下二組の条件を同時に満たすこと：
      - 組I：
        1.  \(A(z_1,\infty)\ne0,|z_1|\ge1\)
        2.  \(A(z_1,z_2)\ne0,|z_1|=1\mbox{ and }|z_2|\ge1\)
      - 組II：
        1.  \(A(\infty,z_2)\ne0,|z_2|\ge1\)
        2.  \(A(z_1,z_2)\ne0,|z_2|=1\mbox{ and }|z_1|\ge1\)
  - （Strintzis）\(T\)にNSSKがない時、因果的伝達関数\(H(z_1,z_2)\)は安定する必要十分条件は
    \*\#\(A(z_1,1)\ne0,|z_1|\ge1\)　しかも
    \*\#\(A(1,z_2)\ne0,|z_2|\ge1\)　しかも
    \*\#\(A(z_1,z_2)\ne0,(z_1,z_2)\in T\)
  - （DeCarlo, Murray and Saeks）\(T\)にNSSKがない時、因果的伝達関数\(H(z_1,z_2)\)は安定する必要十分条件は
    1.  \(A(z_1,z_2)\ne0,|z_1|\ge1\)　しかも
    2.  \(A(z_1,z_2)\ne0,(z_1,z_2)\in T\)

## 参考文献

  - Boaz Porat: *A Course in Digital Signal Processing*, Wiley, ISBN 0-471-14961-6

  - Tamal Bose: *Digital Signal and Image Processing*, Wiley, ISBN 0-471-32727-1

  -
  -
## 関連項目

  - [巡回行列](../Page/巡回行列.md "wikilink")
  - [周波数特性](../Page/周波数特性.md "wikilink")
  - [インパルス応答](https://ja.wikipedia.org/wiki/インパルス応答 "wikilink")
  - [システム解析](https://ja.wikipedia.org/wiki/システム解析 "wikilink")

[Category:電気工学](https://ja.wikipedia.org/wiki/Category:電気工学 "wikilink") [Category:制御理論](https://ja.wikipedia.org/wiki/Category:制御理論 "wikilink") [Category:信号処理](https://ja.wikipedia.org/wiki/Category:信号処理 "wikilink") [Category:解析学](https://ja.wikipedia.org/wiki/Category:解析学 "wikilink") [Category:応用数学](https://ja.wikipedia.org/wiki/Category:応用数学 "wikilink")