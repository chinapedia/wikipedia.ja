> この記事は[可変長配列](https://ja.wikipedia.org/wiki/可変長配列)から翻訳されています。


[プログラミングにおいて](https://ja.wikipedia.org/wiki/プログラミング_\(コンピュータ\) "wikilink")、**可変長配列**（かへんちょうはいれつ、: VLA）とは、[コンパイル](https://ja.wikipedia.org/wiki/コンパイル "wikilink")時ではなく実行時に長さが決定されるの[配列](../Page/配列.md "wikilink")[データ構造](../Page/データ構造.md "wikilink")である\[1\]。

VLAに対応している[プログラミング言語](../Page/プログラミング言語.md "wikilink")には、[Ada](../Page/Ada.md "wikilink")、[ALGOL 68](https://ja.wikipedia.org/wiki/ALGOL#ALGOL_68 "wikilink")（行は固定）、[APL](../Page/APL.md "wikilink")、[C99](../Page/C99.md "wikilink")（ただし[C11ではサポート必須ではなくオプションに格下げとなった](https://ja.wikipedia.org/wiki/C11_\(C言語\) "wikilink")\[2\]\[3\]。またいくつかのプラットフォームにおいて、C99より前のバージョンでは`alloca()`あるいは類似の関数で実装されることがあった）、[C\#](../Page/C_Sharp.md "wikilink")（unsafeモードのスタック割り当て配列として）、[COBOL](../Page/COBOL.md "wikilink")、[Fortran 90](https://ja.wikipedia.org/wiki/Fortran "wikilink")、[J言語がある](../Page/J_\(プログラミング言語\).md "wikilink")。また、[Object Pascal](../Page/Object_Pascal.md "wikilink")（Borland [Delphi](../Page/Delphi.md "wikilink")で使用される言語）でも対応している。

## メモリ

### 割り当て

VLAによって、基本的なメモリ割り当ての問題がユーザーに対して隠蔽される。[ヒープ領域](../Page/ヒープ領域.md "wikilink")と[スタック領域の間に明確な区別がある環境では](https://ja.wikipedia.org/wiki/コールスタック "wikilink")、VLAがどちらのメモリ領域に格納されるかは、ユーザーにはわからない\[4\]。

例えば、[GNU Cコンパイラは](../Page/GNUコンパイラコレクション.md "wikilink")、スタック上にVLAのメモリを割り当てる\[5\]。Cの他のオブジェクトと同様、VLAは`SIZE_MAX`バイトに制限されている\[6\]。

### 変数アクセス

いくつかのプログラミング言語では、[ポインタを介してVLAにアクセスすることができるが](../Page/ポインタ_\(プログラミング\).md "wikilink")、不完全な型と見なされるため、デリファレンス（[間接参照](https://ja.wikipedia.org/wiki/間接参照 "wikilink")）されるとサイズを取得できなくなる\[7\]。

## 例

次の[C99](../Page/C99.md "wikilink")の関数は、指定されたサイズの可変長配列を割り当て、浮動小数点の値で埋めて、別の関数に渡す。配列は自動変数として宣言されているため、`read_and_process()` から戻るとその存続期間が終了する。

``` c
float read_and_process(int n)
{
    float vals[n];

    for (int i = 0; i < n; i++)
        vals[i] = read_val();
    return process(n, vals);
}
```

C99では、関数呼び出しにおける長さのパラメータは、可変長配列のパラメータよりも前に来なければならない\[8\]。

以下は[Ada](../Page/Ada.md "wikilink")における同じ例である。Adaの配列は境界を持っている。従って、長さをProcess関数に渡す必要はない。

``` Ada
type Vals_Type is array (Positive range <>) of Float;

function Read_And_Process (N : Integer) return Float is
   Vals : Vals_Type (1 .. N);
begin
   for I in 1 .. N loop
      Vals (I) := Read_Val;
   end loop;
   return Process (Vals);
end Read_And_Process;
```

[Fortran 90での同等の関数は次の通りである](https://ja.wikipedia.org/wiki/Fortran "wikilink")（コンパイル時にプロシージャ・インターフェイスを検査するFortran 90の機能を利用する場合）。

``` fortran
function read_and_process(n) result(o)
    integer,intent(in)::n
    real::o

    real,dimension(n)::vals
    integer::i

    do i = 1,n
       vals(i) = read_val()
    end do
    o = process(vals)
end function read_and_process
```

一方、関数がFortran 90以前の呼び出しインターフェイスを使用する場合は、まず（外部）関数を宣言し、配列の長さを引数として明示的に渡す必要がある（Cの場合と同様）。

``` fortran
function read_and_process(n) result(o)
    integer,intent(in)::n
    real::o

    real,dimension(n)::vals
    real::read_val, process
    integer::i

    do i = 1,n
       vals(i) = read_val()
    end do
    o = process(vals,n)
end function read_and_process
```

次の[COBOL](../Page/COBOL.md "wikilink")のコード断片は、`PEOPLE-CNT`値で指定された長さ（メンバー数）を持つレコードの可変長配列`DEPT-PERSON`を宣言する。

``` cobolfree
DATA DIVISION.
WORKING-STORAGE SECTION.
01  DEPT-PEOPLE.
    05  PEOPLE-CNT          PIC S9(4) BINARY.
    05  DEPT-PERSON         OCCURS 0 TO 20 TIMES DEPENDING ON PEOPLE-CNT.
        10  PERSON-NAME     PIC X(20).
        10  PERSON-WAGE     PIC S9(7)V99 PACKED-DECIMAL.
```

次の[C\#のコード断片は](../Page/C_Sharp.md "wikilink")、整数型の可変長配列を宣言する。`unsafe`というキーワードは、このコードを含むアセンブリが「安全でない」(unsafe) とマークされることを必要とする\[9\]。

``` csharp
unsafe void declareStackBasedArray(int size)
{
    int *pArray = stackalloc int[size];
    pArray[0] = 123;
}
```

## 動的割り当てと自動割り当て

[Java](https://ja.wikipedia.org/wiki/Java "wikilink")や[.NET Frameworkなどの言語は](https://ja.wikipedia.org/wiki/.NET_Framework "wikilink")、可変長配列を提供するとはみなされない。これらの言語では、全ての配列オブジェクトは論理的に[ヒープ](../Page/ヒープ.md "wikilink")に割り当てられ、配列に関して自動記憶期間というものを持たないためである（JavaとdotNetのコンパイラは、これらのヒープ割り当てを可能な限りスタック上に実際に配置するよう最適化できる）。

## 脚注

[Category:データ構造](https://ja.wikipedia.org/wiki/Category:データ構造 "wikilink")

1.
2.
3.  ISO 9899:2011 Programming Languages - C 6.7.6.2 4
4.
5.
6.  §6.5.3.4 and §7.20.3 of the C11 standard (n1570.pdf)
7.
8.
9.  より具体的にいうと、アセンブリのコンパイルオプションに "/unsafe" を追加する必要がある。